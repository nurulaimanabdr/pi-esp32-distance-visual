#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

// Copy this file to wifi_credentials.h and edit with your network and Pi info.
// Do NOT commit wifi_credentials.h with real credentials to a public repo.

const char* WIFI_SSID = "YOUR_SSID_HERE";
const char* WIFI_PASSWORD = "YOUR_PASSWORD_HERE";

// Raspberry Pi IP - replace with the Pi's LAN IP (e.g., 192.168.1.42)
const char* PI_IP = "192.168.x.x";

// Raspberry Pi port where Flask runs (default in repo: 5000)
const int PI_PORT = 5000;

// Optional: how often to send readings (milliseconds)
const unsigned long SEND_INTERVAL_MS = 200;

#endif // WIFI_CREDENTIALS_H
